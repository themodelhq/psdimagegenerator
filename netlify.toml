[build]
  command = "pnpm run build:client"
  publish = "dist/public"

[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS = "--legacy-peer-deps"

# ── API proxy → Render backend ─────────────────────────────────────────────────
[[redirects]]
  from = "/api/*"
  to = "https://psdimagegenerator.onrender.com/api/:splat"
  status = 200
  force = true

# ── Socket.io proxy → Render backend ──────────────────────────────────────────
[[redirects]]
  from = "/socket.io/*"
  to = "https://psdimagegenerator.onrender.com/socket.io/:splat"
  status = 200
  force = true

# ── SPA fallback ───────────────────────────────────────────────────────────────
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[context.production.environment]
  NODE_ENV = "production"
